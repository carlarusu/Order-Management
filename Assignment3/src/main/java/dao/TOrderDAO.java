package dao;

import model.TOrder;
/**
 *Class extends the abstract data access layer for the model TOrder
 */
public class TOrderDAO  extends AbstractDAO<TOrder> {
	// uses basic CRUD methods from superclass
}
