package dao;

import model.TProduct;
/**
 *Class extends the abstract data access layer for the model TProduct
 */
public class TProductDAO  extends AbstractDAO<TProduct> {
	// uses basic CRUD methods from superclass
}
