package dao;

import model.TClient;
/**
 *Class extends the abstract data access layer for the model TClient
 */
public class TClientDAO extends AbstractDAO<TClient> {

	// uses basic CRUD methods from superclass
}
